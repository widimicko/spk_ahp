  </main>
  
  <!-- Jquery Core -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  <!-- DataTable -->
  <script type="text/javascript" src="https://cdn.datatables.net/v/bs5/dt-1.11.3/datatables.min.js"></script>
  <!-- Chart Js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script>

  if(document.querySelector('#dataTable')) {
    $(document).ready( function () {
      $('#dataTable').DataTable();
    });
  }
  

    const chartElement  = document.getElementById('result-chart')

    if (chartElement) {
      const labels = <%- JSON.stringify(data.alternative); %>
    
      const data = {
        labels: labels,
        datasets: [{
          label: 'Hasil Perhitungan SPK Metode AHP',
          backgroundColor: ['rgb(255, 99, 132)', 'pink', 'yellow', 'cyan', 'green', 'red'],
          borderColor: 'rgb(255, 99, 132)',
          data: <%- result ? JSON.stringify(result.rankedScores) : '' %>,
        }]
      };
    
      const config = {
        type: 'bar',
        data: data,
        options: {
          scales: {
            y: {
              beginAtZero: true
            }
          }
        },
      };
    
      const myChart = new Chart(chartElement, config);
    }

  </script>
</body>
</html>